module.exports = {
  PORT: process.env.PORT || 8000,
  NODE_ENV: process.env.NODE_ENV || "development",
};

// warmup question answers

//How do clients and servers interact?
// clients make requests, servers respond to those requests
//What is a JSON server?
// a server that specifically responds with information in the JSON format
//What does CRUD stand for?
// CREATE, READ, UPDATE, DELETE